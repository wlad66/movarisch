# Server Configuration
PORT=3000
NODE_ENV=production

# JWT Secret Key - CHANGE THIS IN PRODUCTION!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=movarisch
DB_USER=postgres
DB_PASSWORD=your-database-password

# For VPS deployment with Dokploy, you can use:
# DB_HOST=postgres  (if using Dokploy's PostgreSQL service)
# Or connect to existing PostgreSQL instance on VPS
